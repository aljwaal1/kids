name: kids_learning_v4
description: Kids learning app with offline asset sounds and educational games.
publish_to: 'none'
version: 1.0.4+4

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
