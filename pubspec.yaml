name: kids_learning_v1
description: تطبيق طفلي الأول التعليمي للأطفال - عربي وإنجليزي وألعاب.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.3
  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true
